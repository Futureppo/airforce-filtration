# HTTP代理池
PROXIES=

UPSTREAM_URL=https://api.airforce
LISTEN_PORT=6777

# 自定义广告过滤关键词，用 | 分隔（留空则使用默认关键词）
# 默认关键词: Need proxies cheaper than the market? | op.wtf | Upgrade your plan to remove this message | discord.gg/airforce
AD_KEYWORDS=

# 流式响应尾部缓冲区大小（字符数，默认: 400）
AD_BUFFER_SIZE=400
